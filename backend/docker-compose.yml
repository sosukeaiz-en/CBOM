version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////data/ecdat.db
      - LOG_LEVEL=INFO
      - SECRET_KEY=dev_secret_key_ecdat_sih26164
    volumes:
      - ecdat_data:/data
    restart: unless-stopped

volumes:
  ecdat_data:
